/*
import { Router } from 'express'

import { bankAccountsController } from '../controllers'
import { bankAccountsSchemas } from '../schemas'

const bankAccountsRouter = Router()

bankAccountsRouter.get(
  '/bank_accounts/:id',
)

export default bankAccountsRouter
*/